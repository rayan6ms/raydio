diff --git a/crates/oto/src/audio.rs b/crates/oto/src/audio.rs
index 9a5192f..0921b80 100644
--- a/crates/oto/src/audio.rs
+++ b/crates/oto/src/audio.rs
@@ -583,6 +583,7 @@ fn increment(counter: &AtomicU64) {
 
 struct Executor {
     trace: Option<crate::send_trace::TraceWriter>,
+    send_timing: crate::send_trace::SendTiming,
     id: u64,
     source: SourceSlot,
     wake: Arc<WakeShared>,
@@ -628,6 +629,7 @@ async fn run_audio(
     let pacer = input.pacer;
     let mut executor = Executor {
         trace: None,
+        send_timing: Default::default(),
         id: input.id,
         source: SourceSlot::new(input.source, SourceGeneration::FIRST, &wake),
         wake,
@@ -763,6 +765,7 @@ async fn try_start(&mut self) -> Result<(), Error> {
         if self.stop_reply.is_some() || !self.connection_ready() || self.transport.is_none() {
             return Ok(());
         }
+        self.send_timing = Default::default();
         match self.poll_source()? {
             Polled::Frame(len) => {
                 let staged_generation = self.source.generation;
@@ -830,6 +833,11 @@ async fn resume_silence_drain(&mut self) -> Result<(), Error> {
 
     async fn handle_deadline(&mut self, deadline: Instant) -> Result<(), Error> {
         let lateness = Instant::now().saturating_duration_since(deadline);
+        self.send_timing = crate::send_trace::SendTiming {
+            scheduled: true,
+            wake_lateness: crate::send_trace::micros(lateness),
+            ..Default::default()
+        };
         self.store
             .counters
             .skipped_deadlines
@@ -841,8 +849,8 @@ async fn handle_deadline(&mut self, deadline: Instant) -> Result<(), Error> {
 
         // A delayed opportunity still permits one current frame. Dropping the
         // opportunity here adds another timer wait after a host scheduling
-        // stall. The completion path below rebases the next deadline and
-        // replaces the old deadline, so recovery cannot burst a backlog.
+        // stall. The completion path enforces minimum packet spacing while
+        // gradually returning to the media clock, without an immediate backlog.
 
         let polled = if self.stop_reply.is_some() {
             Polled::Ended
@@ -883,8 +891,8 @@ async fn handle_deadline(&mut self, deadline: Instant) -> Result<(), Error> {
             }
         }
         // The opportunity itself, polling, or encryption may be late.
-        // Preserve that valid frame, but start the next opportunity a
-        // full period from completion instead of consuming an old deadline.
+        // Preserve that valid frame and enforce bounded spacing from completion
+        // instead of immediately consuming an overdue deadline.
         if self.active_timeline {
             let completed = Instant::now();
             let lateness = completed.saturating_duration_since(deadline);
@@ -1056,6 +1064,9 @@ fn poll_source(&mut self) -> Result<Polled, Error> {
         let started = StdInstant::now();
         let result = self.source.source.poll_frame(&mut cx, &mut self.frame);
         let wall_elapsed = started.elapsed();
+        if self.trace.is_some() {
+            self.send_timing.source_poll = crate::send_trace::micros(wall_elapsed);
+        }
         if wall_elapsed > SOURCE_POLL_LIMIT
             && matches!(self.source.source, AudioSource::Callback(_))
         {
@@ -1130,6 +1141,8 @@ async fn send_payload(&mut self, len: usize, silence: bool) -> Result<SendOutcom
             .as_mut()
             .and_then(|transport| transport.dave_media.as_mut());
         let dave_payload = if let Some(dave_media) = dave_media {
+            dave_media.measure = self.trace.is_some();
+            let started = self.trace.as_ref().map(|_| StdInstant::now());
             let frame = std::mem::take(&mut self.frame);
             let output = std::mem::take(&mut self.dave_frame);
             let buffers = dave_media
@@ -1144,6 +1157,11 @@ async fn send_payload(&mut self, len: usize, silence: bool) -> Result<SendOutcom
                     )
                     .with_source(source)
                 })?;
+            if let Some(started) = started {
+                self.send_timing.dave_wait = crate::send_trace::micros(started.elapsed());
+                self.send_timing.dave_work_wall = buffers.work_wall_micros;
+                self.send_timing.dave_work_cpu = buffers.work_cpu_micros;
+            }
             self.frame = buffers.frame;
             self.dave_frame = buffers.output;
             let outcome = buffers.result.map_err(|source| {
@@ -1166,6 +1184,7 @@ async fn send_payload(&mut self, len: usize, silence: bool) -> Result<SendOutcom
         let Some(transport) = self.transport.as_mut() else {
             return Ok(SendOutcome::Renewing);
         };
+        let crypto_started = self.trace.as_ref().map(|_| StdInstant::now());
         match transport.encoder.encrypt_next(payload, &mut self.packet) {
             Ok(()) => {}
             Err(TransportCryptoFailure::NonceExhausted) => {
@@ -1182,6 +1201,10 @@ async fn send_payload(&mut self, len: usize, silence: bool) -> Result<SendOutcom
                 ));
             }
         }
+        if let Some(started) = crypto_started {
+            self.send_timing.transport_crypto = crate::send_trace::micros(started.elapsed());
+        }
+        let udp_started = self.trace.as_ref().map(|_| StdInstant::now());
         for attempt in 0..3 {
             #[cfg(test)]
             let injected = self.fail_udp_sends.load(Ordering::Acquire);
@@ -1195,6 +1218,9 @@ async fn send_payload(&mut self, len: usize, silence: bool) -> Result<SendOutcom
             if matches!(result, Ok(Ok(written)) if written == self.packet.len()) {
                 self.staged_frame_len = None;
                 let now = Instant::now();
+                if let Some(started) = udp_started {
+                    self.send_timing.udp_wait = crate::send_trace::micros(started.elapsed());
+                }
                 if let Some(trace) = self.trace.as_mut()
                     && !trace.record(
                         now,
@@ -1202,6 +1228,7 @@ async fn send_payload(&mut self, len: usize, silence: bool) -> Result<SendOutcom
                         transport.generation.get(),
                         self.source.generation.get(),
                         silence,
+                        self.send_timing,
                     )
                 {
                     self.trace = None;
@@ -1603,6 +1630,7 @@ async fn overdue_opportunity_recovers_one_frame_without_waiting_or_catchup() {
         let counters = Arc::new(AudioCounters::default());
         let mut executor = Executor {
             trace: None,
+            send_timing: Default::default(),
             id: 1,
             source: SourceSlot::new(
                 AudioSource::Callback(Box::new(ReadySource)),
@@ -1671,10 +1699,10 @@ async fn overdue_opportunity_recovers_one_frame_without_waiting_or_catchup() {
                 futures_util::FutureExt::now_or_never(executor.pacer.next_deadline()).is_none(),
                 "no catch-up tick retained"
             );
-            tokio::time::advance(FRAME_PERIOD - Duration::from_millis(1)).await;
+            tokio::time::advance(crate::pacer::MIN_SEND_SPACING - Duration::from_millis(1)).await;
             assert!(
                 futures_util::FutureExt::now_or_never(executor.pacer.next_deadline()).is_none(),
-                "next frame must wait a full period"
+                "next frame must respect minimum spacing"
             );
             tokio::time::advance(Duration::from_millis(1)).await;
             tokio::task::yield_now().await;
diff --git a/crates/oto/src/dave.rs b/crates/oto/src/dave.rs
index 0db0889..ac9dab5 100644
--- a/crates/oto/src/dave.rs
+++ b/crates/oto/src/dave.rs
@@ -103,6 +103,7 @@ enum Command {
         len: usize,
         output: Vec<u8>,
         reply: mpsc::Sender<EncryptedBuffers>,
+        measure: bool,
     },
 }
 
@@ -110,6 +111,8 @@ pub(crate) struct EncryptedBuffers {
     pub(crate) frame: Vec<u8>,
     pub(crate) output: Vec<u8>,
     pub(crate) result: Result<MediaOutcome, Failure>,
+    pub(crate) work_wall_micros: u64,
+    pub(crate) work_cpu_micros: u64,
 }
 
 /// Readiness is decided in the same owner turn as encryption. A snapshot read
@@ -124,6 +127,7 @@ pub(crate) struct MediaEncryptor {
     commands: mpsc::Sender<Command>,
     reply: mpsc::Sender<EncryptedBuffers>,
     responses: mpsc::Receiver<EncryptedBuffers>,
+    pub(crate) measure: bool,
 }
 
 impl std::fmt::Debug for MediaEncryptor {
@@ -163,7 +167,11 @@ fn spawn_core(core: Core, capacity: usize) -> Self {
                         len,
                         mut output,
                         reply,
+                        measure,
                     } => {
+                        let wall = measure.then(std::time::Instant::now);
+                        #[cfg(target_os = "linux")]
+                        let cpu = measure.then(crate::audio::source_cpu_time);
                         let result = if core.snapshot().ready {
                             core.encrypt_into(&frame[..len], &mut output)
                                 .map(|()| MediaOutcome::Encrypted)
@@ -171,11 +179,23 @@ fn spawn_core(core: Core, capacity: usize) -> Self {
                             output.clear();
                             Ok(MediaOutcome::NotReady)
                         };
+                        #[cfg(target_os = "linux")]
+                        let work_cpu_micros = cpu.map_or(0, |start| {
+                            crate::send_trace::micros(
+                                crate::audio::source_cpu_time().saturating_sub(start),
+                            )
+                        });
+                        #[cfg(not(target_os = "linux"))]
+                        let work_cpu_micros = u64::MAX;
+                        let work_wall_micros =
+                            wall.map_or(0, |start| crate::send_trace::micros(start.elapsed()));
                         let _ = reply
                             .send(EncryptedBuffers {
                                 frame,
                                 output,
                                 result,
+                                work_wall_micros,
+                                work_cpu_micros,
                             })
                             .await;
                     }
@@ -205,6 +225,7 @@ pub(crate) fn media_encryptor(&self) -> MediaEncryptor {
             commands: self.inner.commands.clone(),
             reply,
             responses,
+            measure: false,
         }
     }
 
@@ -243,6 +264,7 @@ pub(crate) async fn encrypt_buffered(
                 len,
                 output,
                 reply: self.reply.clone(),
+                measure: self.measure,
             }),
         )
         .await
@@ -1374,6 +1396,7 @@ async fn media_diagnostics_distinguish_queue_and_response_deadlines() {
             commands,
             reply,
             responses,
+            measure: false,
         };
         media
             .commands
@@ -1381,6 +1404,7 @@ async fn media_diagnostics_distinguish_queue_and_response_deadlines() {
                 frame: vec![1],
                 len: 1,
                 output: Vec::new(),
+                measure: false,
                 reply: media.reply.clone(),
             })
             .await
@@ -1413,6 +1437,7 @@ async fn cancelled_media_response_cannot_block_owner_and_last_handle_drop_closes
                 frame: vec![1, 2, 3],
                 len: 3,
                 output: Vec::new(),
+                measure: false,
                 reply,
             })
             .await
@@ -1448,6 +1473,7 @@ async fn owner_orders_media_across_execute_without_partial_visibility() {
                 frame: frame.clone(),
                 len: frame.len(),
                 output: Vec::new(),
+                measure: false,
                 reply: before_reply,
             })
             .await
@@ -1468,6 +1494,7 @@ async fn owner_orders_media_across_execute_without_partial_visibility() {
                 frame: frame.clone(),
                 len: frame.len(),
                 output: Vec::new(),
+                measure: false,
                 reply: after_reply,
             })
             .await
diff --git a/crates/oto/src/lib.rs b/crates/oto/src/lib.rs
index 383d0c5..418b254 100644
--- a/crates/oto/src/lib.rs
+++ b/crates/oto/src/lib.rs
@@ -49,4 +49,4 @@ pub fn ip_discovery_response(input: &[u8]) {
     ConnectionPhase, ConnectionSnapshot, ConnectionStats, EventReceiveError, FailureSnapshot,
     SourceGeneration, VoiceConnectInfo, VoiceToken,
 };
-pub use send_trace::{SendRecord, SendTrace};
+pub use send_trace::{SendRecord, SendTiming, SendTrace};
diff --git a/crates/oto/src/pacer.rs b/crates/oto/src/pacer.rs
index 236f8f8..e479b4a 100644
--- a/crates/oto/src/pacer.rs
+++ b/crates/oto/src/pacer.rs
@@ -5,6 +5,12 @@
 use tokio::time::{Instant, sleep_until};
 
 pub(crate) const FRAME_PERIOD: Duration = Duration::from_millis(20);
+// Recover modest scheduling debt without sending an immediate backlog. A
+// With on-time wakeups and negligible work, a 60 ms stall recovers over 540 ms
+// rather than shifting every later media frame by 60 ms. Long outages retain
+// at most 100 ms of recovery debt.
+pub(crate) const MIN_SEND_SPACING: Duration = Duration::from_millis(18);
+const MAX_RECOVERY_DEBT: Duration = Duration::from_millis(100);
 
 /// The audio executor owns its timer, avoiding a coordinator/channel wakeup
 /// for every packet. No extra task, thread, or queue is created per sender.
@@ -21,6 +27,7 @@ pub(crate) enum PacerFailure {
 
 pub(crate) struct PacerRegistration {
     next: Option<Instant>,
+    ideal: Option<Instant>,
     skipped: u64,
     observed_missed: u64,
     registered: bool,
@@ -43,6 +50,7 @@ pub(crate) async fn register(&self) -> Result<PacerRegistration, PacerFailure> {
         // Registration has no suspension point and no shared slot to roll back.
         Ok(PacerRegistration {
             next: None,
+            ideal: None,
             skipped: 0,
             observed_missed: 0,
             registered: true,
@@ -65,16 +73,14 @@ pub(crate) async fn next_deadline(&mut self) -> Option<Instant> {
         let missed = now.saturating_duration_since(deadline).as_nanos() / FRAME_PERIOD.as_nanos();
         self.observed_missed = missed.min(u128::from(u64::MAX)) as u64;
         self.skipped = self.skipped.saturating_add(self.observed_missed);
-        self.next = Some(if missed > 0 {
-            now + FRAME_PERIOD
-        } else {
-            deadline + FRAME_PERIOD
-        });
+        self.ideal = Some(self.ideal.unwrap_or(deadline) + FRAME_PERIOD);
+        self.schedule_after(now);
         Some(deadline)
     }
 
     /// Include opportunities missed during polling/encryption/I/O, without
-    /// counting the scheduling delay twice. Completion may also rebase the timer.
+    /// counting the scheduling delay twice. Every completed packet gets a
+    /// minimum spacing before the next opportunity, including sub-period stalls.
     pub(crate) fn complete(&mut self, deadline: Instant, completed: Instant) {
         let missed = (completed.saturating_duration_since(deadline).as_nanos()
             / FRAME_PERIOD.as_nanos())
@@ -83,9 +89,16 @@ pub(crate) fn complete(&mut self, deadline: Instant, completed: Instant) {
             .skipped
             .saturating_add(missed.saturating_sub(self.observed_missed));
         self.observed_missed = 0;
-        if missed > 0 {
-            self.next = Some(completed + FRAME_PERIOD);
-        }
+        self.ideal.get_or_insert(deadline + FRAME_PERIOD);
+        self.schedule_after(completed);
+    }
+
+    fn schedule_after(&mut self, completed: Instant) {
+        let ideal = self.ideal.expect("active sender has a media deadline");
+        let oldest = completed.checked_sub(MAX_RECOVERY_DEBT).unwrap_or(ideal);
+        let ideal = ideal.max(oldest);
+        self.ideal = Some(ideal);
+        self.next = Some(ideal.max(completed + MIN_SEND_SPACING));
     }
 
     pub(crate) async fn activate(&mut self, first: Instant) -> Result<(), PacerFailure> {
@@ -93,12 +106,14 @@ pub(crate) async fn activate(&mut self, first: Instant) -> Result<(), PacerFailu
             return Err(PacerFailure::Closed);
         }
         self.next = Some(first);
+        self.ideal = Some(first);
         self.observed_missed = 0;
         Ok(())
     }
 
     pub(crate) async fn deactivate(&mut self) -> Result<(), PacerFailure> {
         self.next = None;
+        self.ideal = None;
         Ok(())
     }
 
@@ -108,6 +123,7 @@ pub(crate) fn skipped(&self) -> u64 {
 
     pub(crate) async fn unregister(&mut self) -> Result<(), PacerFailure> {
         self.next = None;
+        self.ideal = None;
         self.registered = false;
         Ok(())
     }
@@ -118,6 +134,59 @@ mod tests {
     use super::*;
     use futures_util::FutureExt;
 
+    #[tokio::test(start_paused = true)]
+    async fn recovery_preserves_frames_and_returns_to_the_original_media_clock() {
+        for delay_ms in [1, 19, 20, 37, 60, 80] {
+            let mut p = Pacer::new().register().await.unwrap();
+            let first = Instant::now() + FRAME_PERIOD;
+            p.activate(first).await.unwrap();
+            tokio::time::advance(FRAME_PERIOD + Duration::from_millis(delay_ms)).await;
+            assert_eq!(p.next_deadline().await, Some(first));
+            let mut previous = Instant::now();
+            p.complete(first, previous);
+            for frame in 1..=60 {
+                assert!(p.next_deadline().now_or_never().is_none());
+                let deadline = p.next_deadline().await.unwrap();
+                let sent = Instant::now();
+                assert!(sent.duration_since(previous) >= MIN_SEND_SPACING);
+                assert!(sent.duration_since(previous) <= FRAME_PERIOD);
+                assert!(sent >= first + FRAME_PERIOD * frame);
+                p.complete(deadline, sent);
+                previous = sent;
+            }
+            assert_eq!(
+                previous,
+                first + FRAME_PERIOD * 60,
+                "all 61 sequential frames must reach the original media clock"
+            );
+        }
+    }
+
+    #[tokio::test(start_paused = true)]
+    async fn long_outages_bound_recovery_and_deactivation_clears_its_debt() {
+        let mut p = Pacer::new().register().await.unwrap();
+        let first = Instant::now();
+        p.activate(first).await.unwrap();
+        tokio::time::advance(Duration::from_secs(5)).await;
+        assert_eq!(p.next_deadline().await, Some(first));
+        let mut previous = Instant::now();
+        p.complete(first, previous);
+        for _ in 0..70 {
+            let deadline = p.next_deadline().await.unwrap();
+            let sent = Instant::now();
+            assert!(sent.duration_since(previous) >= MIN_SEND_SPACING);
+            p.complete(deadline, sent);
+            previous = sent;
+        }
+        assert_eq!(p.next.unwrap(), previous + FRAME_PERIOD);
+        p.deactivate().await.unwrap();
+        let fresh = Instant::now() + FRAME_PERIOD;
+        p.activate(fresh).await.unwrap();
+        assert_eq!(p.next_deadline().await, Some(fresh));
+        p.complete(fresh, Instant::now());
+        assert_eq!(p.next.unwrap(), fresh + FRAME_PERIOD);
+    }
+
     #[tokio::test(start_paused = true)]
     async fn cancelled_wait_does_not_advance_or_lose_deadline() {
         let mut p = Pacer::new().register().await.unwrap();
@@ -140,7 +209,7 @@ async fn delayed_executor_recovers_once_without_catchup() {
         assert_eq!(p.next_deadline().await, Some(first));
         assert_eq!(p.skipped(), 7);
         assert!(p.next_deadline().now_or_never().is_none());
-        let next = Instant::now() + FRAME_PERIOD;
+        let next = Instant::now() + MIN_SEND_SPACING;
         assert_eq!(p.next_deadline().await, Some(next));
     }
 
@@ -193,7 +262,7 @@ async fn slow_frame_work_counts_missed_periods_without_double_counting() {
         assert!(p.next_deadline().now_or_never().is_none());
         assert_eq!(
             p.next_deadline().await,
-            Some(first + Duration::from_millis(105))
+            Some(first + Duration::from_millis(103))
         );
     }
 }
diff --git a/crates/oto/src/send_trace.rs b/crates/oto/src/send_trace.rs
index eede817..8f7af66 100644
--- a/crates/oto/src/send_trace.rs
+++ b/crates/oto/src/send_trace.rs
@@ -4,6 +4,50 @@
 use std::time::SystemTime;
 use tokio::time::Instant;
 
+/// Optional send-stage measurements, enabled only while a send trace is active.
+/// All values are microseconds. Wall durations include descheduling; DAVE CPU
+/// measures only the owner thread's encryption work on Linux.
+#[derive(Clone, Copy, Debug, Default, PartialEq, Eq)]
+pub struct SendTiming {
+    /// False for an initial or readiness-driven send without a timer deadline.
+    pub scheduled: bool,
+    /// Delay between the scheduled deadline and its handler starting.
+    pub wake_lateness: u64,
+    /// Wall time spent polling for the encoded source frame.
+    pub source_poll: u64,
+    /// Full DAVE command/response round trip, including queue waits.
+    pub dave_wait: u64,
+    /// Wall time inside the DAVE owner, excluding command/response queue waits.
+    pub dave_work_wall: u64,
+    /// `u64::MAX` when the platform cannot measure thread CPU time.
+    pub dave_work_cpu: u64,
+    /// Wall time encrypting the RTP transport payload.
+    pub transport_crypto: u64,
+    /// Wall time submitting UDP, including any retry delays.
+    pub udp_wait: u64,
+}
+
+impl SendTiming {
+    /// Stable trace schema: scheduled (0/1), wake lateness, source poll,
+    /// DAVE round trip, DAVE work wall/CPU, transport crypto, UDP wait.
+    pub fn as_micros(self) -> [u64; 8] {
+        [
+            u64::from(self.scheduled),
+            self.wake_lateness,
+            self.source_poll,
+            self.dave_wait,
+            self.dave_work_wall,
+            self.dave_work_cpu,
+            self.transport_crypto,
+            self.udp_wait,
+        ]
+    }
+}
+
+pub(crate) fn micros(duration: std::time::Duration) -> u64 {
+    duration.as_micros().min(u128::from(u64::MAX)) as u64
+}
+
 /// One successfully submitted UDP packet. Success is local submission, not delivery.
 #[derive(Clone, Copy, Debug, PartialEq, Eq)]
 pub struct SendRecord {
@@ -23,6 +67,8 @@ pub struct SendRecord {
     pub sequence: u16,
     /// Whether this was one of Oto's terminal/underflow silence packets.
     pub silence: bool,
+    /// Stage timings for this packet, excluding work before tracing was enabled.
+    pub timing: SendTiming,
 }
 
 /// Single-consumer trace. Drain outside the audio task; dropping it disables tracing.
@@ -82,6 +128,7 @@ pub(crate) fn record(
         connection: u64,
         source: u64,
         silence: bool,
+        timing: SendTiming,
     ) -> bool {
         if self.producer.is_abandoned() {
             return false;
@@ -99,6 +146,7 @@ pub(crate) fn record(
             timestamp: u32::from_be_bytes(header[4..8].try_into().expect("RTP timestamp")),
             sequence: u16::from_be_bytes(header[2..4].try_into().expect("RTP sequence")),
             silence,
+            timing,
         };
         if self.producer.push(record).is_err() {
             self.dropped.fetch_add(1, Ordering::Relaxed);
@@ -115,8 +163,18 @@ async fn bounded_trace_preserves_headers_and_exposes_missing_records() {
         let (mut writer, mut reader) = pair(1);
         let header = [0x80, 0x78, 0xff, 0xff, 0, 0, 3, 0xc0, 0, 0, 0, 7];
         let now = Instant::now();
-        assert!(writer.record(now, &header, 4, 8, false));
-        assert!(writer.record(now, &header, 4, 8, false));
+        let timing = SendTiming {
+            scheduled: true,
+            wake_lateness: 33000,
+            source_poll: 2,
+            dave_wait: 120,
+            dave_work_wall: 90,
+            dave_work_cpu: 80,
+            transport_crypto: 3,
+            udp_wait: 12,
+        };
+        assert!(writer.record(now, &header, 4, 8, false, timing));
+        assert!(writer.record(now, &header, 4, 8, false, SendTiming::default()));
         assert_eq!(reader.dropped(), 1);
         let first = reader.pop().unwrap();
         assert_eq!(
@@ -128,13 +186,14 @@ async fn bounded_trace_preserves_headers_and_exposes_missing_records() {
             (4, 8)
         );
         assert!(!first.silence);
+        assert_eq!(first.timing.as_micros(), [1, 33000, 2, 120, 90, 80, 3, 12]);
         tokio::time::advance(std::time::Duration::from_millis(20)).await;
-        writer.record(Instant::now(), &header, 5, 9, true);
+        writer.record(Instant::now(), &header, 5, 9, true, SendTiming::default());
         let third = reader.pop().unwrap();
         assert_eq!(third.index, 3);
         assert_eq!(third.elapsed_micros, 20000);
         assert!(third.silence);
         drop(reader);
-        assert!(!writer.record(Instant::now(), &header, 5, 9, false));
+        assert!(!writer.record(Instant::now(), &header, 5, 9, false, SendTiming::default()));
     }
 }
